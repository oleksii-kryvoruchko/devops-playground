replicaCount: 2

image:
  repository: ghcr.io/oleksii-kryvoruchko/devops-playground
  tag: d004c560f396a79b9a66c9a5057c8ef15e521a62
  pullPolicy: IfNotPresent

ingress:
  enabled: true
  hosts:
    - host: load-tester.local

service:
  type: ClusterIP
  port: 8080

resources:
  requests:
    memory: 128Mi
    cpu: 100m
  limits:
    memory: 1Gi
    cpu: 1000m

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80 
  targetMemoryUtilizationPercentage: 80
